[pytest]
addopts = -vv -s --durations=0 --durations-min=0
python_files = test_*.py benchmarking.py
python_functions = test_*
markers =
    integration: marks MP-SPDZ integration tests
    benchmark: marks benchmark-style scaling tests
